---
// Site header + navigation (desktop and mobile). Acceptance-critical:
// renders on every page via BaseLayout (BRIEF.md §8).
import logoMark from '../assets/logo/rml-mark.svg';

const navItems = [
  { href: '/services', label: 'Services' },
  { href: '/work', label: 'Work' },
  { href: '/about', label: 'About' },
  { href: '/blog', label: 'Blog' },
  { href: '/resources', label: 'Resources' },
  { href: '/consultations', label: 'Consultations' },
];

const serviceItems = [
  { href: '/services/strategy-advisory', label: 'Strategy & Advisory' },
  { href: '/services/research-analytics', label: 'Research & Data Analytics' },
  { href: '/services/mel', label: 'Monitoring, Evaluation & Learning' },
];

const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (href: string) =>
  href === '/' ? currentPath === '/' : currentPath.startsWith(href);
---

<header class="sticky top-0 z-40 border-b border-slate-200 bg-white">
  <div class="mx-auto flex h-16 max-w-6xl items-center justify-between px-4 sm:px-6">
    <a href="/" class="flex items-center gap-2.5 text-brand-900" aria-label="Results Matrix Limited — home">
      <img src={logoMark.src} alt="" width="38" height="36" class="h-9 w-auto" />
      <span class="flex items-baseline gap-1.5">
        <span class="text-lg font-bold tracking-tight">Results Matrix</span>
        <span class="hidden text-sm font-medium text-accent-600 sm:inline">Limited</span>
      </span>
    </a>

    <!-- Desktop nav -->
    <nav class="hidden items-center gap-1 lg:flex" aria-label="Main">
      {
        navItems.map((item) => (
          <a
            href={item.href}
            aria-current={isActive(item.href) ? 'page' : undefined}
            class:list={[
              'rounded px-3 py-2 text-sm font-medium transition-colors',
              isActive(item.href)
                ? 'text-accent-700'
                : 'text-slate-600 hover:text-brand-900',
            ]}
          >
            {item.label}
          </a>
        ))
      }
      <a
        href="/contact"
        class="ml-2 rounded bg-brand-800 px-4 py-2 text-sm font-semibold text-white transition-colors hover:bg-brand-700"
      >
        Contact us
      </a>
    </nav>

    <!-- Mobile nav toggle -->
    <button
      type="button"
      id="nav-toggle"
      class="-mr-2 inline-flex h-11 w-11 items-center justify-center rounded text-brand-900 lg:hidden"
      aria-expanded="false"
      aria-controls="mobile-nav"
    >
      <span class="sr-only">Open main menu</span>
      <svg id="nav-icon-open" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
      </svg>
      <svg id="nav-icon-close" class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <!-- Mobile nav panel -->
  <nav id="mobile-nav" class="hidden border-t border-slate-200 bg-white lg:hidden" aria-label="Main menu">
    <div class="mx-auto max-w-6xl space-y-1 px-4 py-4 sm:px-6">
      {
        navItems.map((item) => (
          <>
            <a
              href={item.href}
              aria-current={isActive(item.href) ? 'page' : undefined}
              class:list={[
                'block rounded px-3 py-2.5 text-base font-medium',
                isActive(item.href)
                  ? 'bg-accent-50 text-accent-700'
                  : 'text-slate-700 hover:bg-slate-50',
              ]}
            >
              {item.label}
            </a>
            {item.href === '/services' &&
              serviceItems.map((sub) => (
                <a
                  href={sub.href}
                  aria-current={isActive(sub.href) ? 'page' : undefined}
                  class:list={[
                    'block rounded py-2 pl-8 pr-3 text-sm',
                    isActive(sub.href)
                      ? 'bg-accent-50 text-accent-700'
                      : 'text-slate-600 hover:bg-slate-50',
                  ]}
                >
                  {sub.label}
                </a>
              ))}
          </>
        ))
      }
      <a href="/contact" class="mt-2 block rounded bg-brand-800 px-4 py-2.5 text-center text-base font-semibold text-white hover:bg-brand-700">
        Contact us
      </a>
    </div>
  </nav>
</header>

<script>
  const toggle = document.getElementById('nav-toggle')!;
  const panel = document.getElementById('mobile-nav')!;
  const iconOpen = document.getElementById('nav-icon-open')!;
  const iconClose = document.getElementById('nav-icon-close')!;

  function setOpen(open: boolean) {
    toggle.setAttribute('aria-expanded', String(open));
    panel.classList.toggle('hidden', !open);
    iconOpen.classList.toggle('hidden', open);
    iconClose.classList.toggle('hidden', !open);
  }

  toggle.addEventListener('click', () => {
    setOpen(toggle.getAttribute('aria-expanded') !== 'true');
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && toggle.getAttribute('aria-expanded') === 'true') {
      setOpen(false);
      toggle.focus();
    }
  });

  // Reset the panel if the viewport grows past the lg breakpoint.
  matchMedia('(min-width: 64rem)').addEventListener('change', (e) => {
    if (e.matches) setOpen(false);
  });
</script>
